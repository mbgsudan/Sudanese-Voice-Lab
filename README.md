# منصة صوتنا - Sawtna Voice Lab

منصة عربية حديثة لجمع التسجيلات الصوتية السودانية لتدريب نماذج الذكاء الاصطناعي

## 🎯 المميزات الرئيسية

### ✅ **واجهة عربية كاملة**
- جميع النصوص والأزرار باللغة العربية
- تخطيط RTL (من اليمين إلى اليسار) كامل
- خطوط عربية حديثة (Cairo و Tajawal)

### 🎨 **تصميم عصري ومتجاوب**
- ثيم داكن مع ألوان أخضر وتركوازي نيون
- تصميم متجاوب يبدو كتطبيق محمول
- رسوم متحركة سلسة وتأثيرات بصرية

### 🎤 **نظام تسجيل متطور**
- تسجيل صوتي بجودة عالية
- مؤشر مرئي للموجات الصوتية
- مراجعة وتشغيل التسجيل قبل الإرسال
- إدارة المتحدثين والمعلومات الشخصية

### 📊 **نظام متابعة شامل**
- تتبع حالة التسجيلات (قيد المراجعة/مقبول/مرفوض)
- إحصائيات شخصية للمستخدمين
- تصفية وبحث في التسجيلات
- إمكانية إعادة إرسال التسجيلات المرفوضة

### ⚙️ **لوحة إدارة متكاملة**
- حماية بكلمة مرور (`70003mbgz`)
- مراجعة وقبول/رفض التسجيلات
- إدارة المتحدثين
- إحصائيات ورسوم بيانية تفاعلية

## 📁 هيكل الملفات

```
منصة-صوتنا/
├── index.html          # الصفحة الرئيسية
├── record.html         # صفحة التسجيل
├── follow-up.html      # صفحة المتابعة
├── admin.html          # صفحة المراجعة (الإدارة)
├── styles.css          # الأنماط الرئيسية
├── record.css          # أنماط صفحة التسجيل
├── follow-up.css       # أنماط صفحة المتابعة
├── admin.css           # أنماط صفحة الإدارة
├── script.js           # الوظائف العامة
├── home.js            # وظائف الصفحة الرئيسية
├── record.js          # وظائف التسجيل
├── follow-up.js       # وظائف المتابعة
├── admin.js           # وظائف الإدارة
├── database-schema.sql # مخطط قاعدة البيانات
└── README.md          # هذا الملف
```

## 🚀 إعداد المشروع

### 1. إعداد Supabase

1. إنشئ مشروع جديد في [Supabase](https://supabase.com)
2. نسخ URL المشروع ومفتاح API
3. تحديث الإعدادات في `script.js` (مُحدثة بالفعل):

```javascript
const SUPABASE_URL = 'https://qcctqvmwwpsoiexgdqwp.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFjY3Rxdm13d3Bzb2lleGdkcXdwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjI3MjI1OTcsImV4cCI6MjA3ODI5ODU5N30.uTfskCuzkZNcvy1QdaOzqlW8km-wcZQoVRFi6k2xndQ';
```

### 2. إعداد قاعدة البيانات

1. اذهب إلى Supabase Dashboard → SQL Editor
2. تشغيل الأوامر التالية:

```sql
-- إنشاء جدول المتحدثين
CREATE TABLE speakers (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    name TEXT NOT NULL,
    gender TEXT NOT NULL CHECK (gender IN ('male', 'female')),
    age TEXT NOT NULL,
    dialect TEXT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT now()
);

-- إنشاء جدول التسجيلات
CREATE TABLE recordings (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    speaker_id UUID REFERENCES speakers(id) ON DELETE CASCADE,
    text TEXT NOT NULL,
    file_path TEXT NOT NULL,
    status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
    duration INTEGER DEFAULT 0,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT now(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT now()
);

-- تفعيل RLS
ALTER TABLE speakers ENABLE ROW LEVEL SECURITY;
ALTER TABLE recordings ENABLE ROW LEVEL SECURITY;

-- إنشاء السياسات
CREATE POLICY "Enable all access for speakers" ON speakers FOR ALL USING (true);
CREATE POLICY "Enable all access for recordings" ON recordings FOR ALL USING (true);
```

### 3. إعداد التخزين

1. اذهب إلى Supabase Dashboard → Storage
2. إنشئ bucket جديد باسم `recordings`
3. اجعله عاماً (public)

### 4. رفع الملفات

1. رفع `logo.png` و `0dfc3b34-2283-4e1b-aa3a-ebd3e53b5d63.png`
2. تشغيل الموقع على خادم محلي (مطلوب لميزة التسجيل)

```bash
# باستخدام Python
python -m http.server 8000

# أو باستخدام Node.js
npx serve .

# أو باستخدام Live Server في VS Code
```

## 📱 الصفحات والوظائف

### 🏠 الصفحة الرئيسية (`index.html`)
- **الهدف**: عرض المشروع وجذب المساهمين
- **المحتوى**:
  - شعار وعنوان المشروع
  - الشعار: "أصوات السودان تصنع مستقبل الذكاء الاصطناعي"
  - أزرار: "ابدأ التسجيل الآن" و "تابع تسجيلاتك"
  - قسم المميزات والإحصائيات

### 🎤 صفحة التسجيل (`record.html`)
- **الهدف**: تسجيل المساهمات الصوتية
- **الوظائف**:
  - اختيار المتحدث من قائمة أو إضافة جديد
  - عرض نص عربي للقراءة
  - واجهة تسجيل مع مؤشر مرئي
  - مراجعة التسجيل قبل الإرسال
  - رسالة تأكيد الإرسال

### 📊 صفحة المتابعة (`follow-up.html`)
- **الهدف**: تتبع حالة المساهمات
- **الوظائف**:
  - عرض جميع تسجيلات المستخدم
  - فلترة حسب المتحدث والحالة
  - إحصائيات شخصية
  - تشغيل التسجيلات
  - إعادة إرسال المرفوضة

### ⚙️ صفحة المراجعة (`admin.html`)
- **الهدف**: إدارة المنصة
- **كلمة المرور**: `70003mbgz`
- **الوظائف**:
  - مراجعة التسجيلات (قبول/رفض)
  - إدارة المتحدثين
  - عرض الإحصائيات والرسوم البيانية
  - تشغيل التسجيلات للمراجعة

## 🎯 النصوص المستخدمة

المنصة تحتوي على 20 نص عربي سوداني يغطي:
- الجغرافيا والثقافة السودانية
- التاريخ والتراث
- الحياة اليومية
- اللهجات المحلية

## 🔧 المتطلبات التقنية

### **المتصفحات المدعومة**:
- Chrome 60+
- Firefox 55+
- Safari 11+
- Edge 79+

### **المتطلبات**:
- إذن الميكروفون (للتسجيل)
- اتصال إنترنت مستقر
- دعم HTML5 و Web Audio API

## 📊 قاعدة البيانات

### **جدول المتحدثين (speakers)**:
- `id`: المعرف الفريد
- `name`: اسم المتحدث
- `gender`: الجنس (ذكر/أنثى)
- `age`: الفئة العمرية
- `dialect`: اللهجة
- `created_at`: تاريخ التسجيل

### **جدول التسجيلات (recordings)**:
- `id`: المعرف الفريد
- `speaker_id`: مرجع للمتحدث
- `text`: النص المقروء
- `file_path`: مسار ملف الصوت
- `status`: الحالة (قيد المراجعة/مقبول/مرفوض)
- `duration`: مدة التسجيل بالثواني
- `created_at`: تاريخ الإنشاء
- `updated_at`: تاريخ آخر تحديث

## 🎵 مواصفات الصوت

- **الصيغة**: WebM مع Opus codec
- **معدل العينة**: 44.1 kHz
- **المميزات**: إلغاء الصدى، تقليل الضوضاء، تحكم تلقائي بالصوت
- **التخزين**: Supabase Storage مع روابط موقعة آمنة

## 🔒 الأمان والخصوصية

- Row Level Security (RLS) مفعل على جميع الجداول
- رفع آمن للملفات إلى Supabase Storage
- مصادقة الإدارة مع إدارة الجلسة
- روابط موقعة لتشغيل الملفات الصوتية

## 🎨 التصميم المتجاوب

الموقع محسن بالكامل لـ:
- **الهواتف المحمولة** (الاتجاهين)
- **الأجهزة اللوحية**
- **أجهزة الحاسوب**
- **الشاشات الكبيرة**

## 📝 دعم اللغة العربية

- **تخطيط RTL كامل**
- **عرض النص العربي السليم**
- **تنسيق النماذج المناسب للعربية**
- **تنسيق التاريخ والوقت بالعربية**

## 👨‍💻 التطوير

لتطوير الموقع محلياً:

1. استنسخ الملفات
2. شغل خادم محلي
3. افتح المتصفح على `localhost`
4. تأكد من إعدادات Supabase

## 🤝 المساهمة

هذا المشروع يهدف لجمع البيانات الصوتية لأبحاث الذكاء الاصطناعي. المساهمون يساعدون عبر:

1. **تسجيل النصوص العربية**
2. **توفير معلومات ديموغرافية دقيقة**
3. **ضمان النطق الواضح والطبيعي**

## 📞 التواصل

**منشئ المشروع**: محمد بابكر

**الهدف**: تطوير تقنيات الذكاء الاصطناعي للغة العربية السودانية

## 📄 الرخصة

هذا المشروع مصمم لأغراض البحث في التعرف على الكلام العربي ومعالجة اللغات الطبيعية.

---

**منصة صوتنا - أصوات السودان تصنع مستقبل الذكاء الاصطناعي** 🇸🇩